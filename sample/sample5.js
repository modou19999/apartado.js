// -Operadores de asignacion

const x = 22;

// -Operadores de

let result;
result = 2 + 2; // suma
result = 2 - 2; // resta
result = 2 * 2; // multiplicación
result = 3 / 2; // división
result = 3 % 2; // módulo (resto de la división) & = y resto
result = 3 ** 2;

// -Operadores de asignasion mas aritmiticos
const a = 2;

// -Operadores de unarios

a++;
a--; // tambie se puede hacer
// a = a + 1
//a += 1

// -Operadores de cadena (string)
const text = 'Pepe' + ' ' + 'Perez';

// -Operadores de comparacion igualdad (True) or (False)
// iagualdad estricta
// los NaN ??? Es un valor numerico de valor indeterminado
// los ceros
{
    const x = 1;
    const y = -1;
    console.log('comparando con signo', x === y);
}

// -Operadores de bit a bit no vamos a ver
