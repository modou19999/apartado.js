// mutable
userContact.splice();

// inmutables

// Qué es forEach()?
// forEach() es un método de los arrays en JavaScript que ejecuta una función una vez por cada elemento del array.
userContact.forEach((item) => console.log(item));

// Parámetros:
// elemento → valor actual
// indice → posición
// arrayCompleto → el array completo

