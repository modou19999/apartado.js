🔴 MÉTODOS QUE MUTAN (modifican el array original)
🔹 push()

👉 Agrega elemento(s) al final.

let frutas = ["manzana", "pera"];

frutas.push("uva");

console.log(frutas); // ["manzana", "pera", "uva"]


✔ Modifica el array original
✔ Devuelve la nueva longitud

🔹 pop()

👉 Elimina el último elemento.

let frutas = ["manzana", "pera", "uva"];

let eliminado = frutas.pop();

console.log(frutas);   // ["manzana", "pera"]
console.log(eliminado); // "uva"


✔ Modifica el array
✔ Devuelve el elemento eliminado

🔹 unshift()

👉 Agrega elemento(s) al inicio.

let frutas = ["pera", "uva"];

frutas.unshift("manzana");

console.log(frutas); // ["manzana", "pera", "uva"]


✔ Modifica el array
✔ Devuelve la nueva longitud

🔹 shift()

👉 Elimina el primer elemento.

let frutas = ["manzana", "pera", "uva"];

let eliminado = frutas.shift();

console.log(frutas);   // ["pera", "uva"]
console.log(eliminado); // "manzana"


✔ Modifica el array
✔ Devuelve el elemento eliminado

🔹 sort()

👉 Ordena el array (lo modifica).

let numeros = [3, 1, 2];

numeros.sort();

console.log(numeros); // [1, 2, 3]


⚠ Si son números grandes, usar comparación:

numeros.sort((a, b) => a - b);


✔ Modifica el array

🔹 reverse()

👉 Invierte el orden.

let numeros = [1, 2, 3];

numeros.reverse();

console.log(numeros); // [3, 2, 1]


✔ Modifica el array

🟢 MÉTODOS QUE NO MUTAN (devuelven nuevo valor)
🔹 toSorted()

👉 Ordena SIN modificar el original.

let numeros = [3, 1, 2];

let ordenado = numeros.toSorted();

console.log(numeros);  // [3, 1, 2]
console.log(ordenado); // [1, 2, 3]


✔ No modifica
✔ Devuelve nuevo array

🔹 forEach()

👉 Recorre el array.

let numeros = [1, 2, 3];

numeros.forEach(n => console.log(n));


✔ No devuelve nada (undefined)
✔ No muta (a menos que tú lo hagas dentro)

🔹 map()

👉 Transforma cada elemento.

let numeros = [1, 2, 3];

let dobles = numeros.map(n => n * 2);

console.log(dobles); // [2, 4, 6]


✔ No modifica
✔ Devuelve nuevo array

🔹 filter()

👉 Filtra elementos según condición.

let numeros = [1, 2, 3, 4];

let pares = numeros.filter(n => n % 2 === 0);

console.log(pares); // [2, 4]


✔ No modifica
✔ Devuelve nuevo array

🔹 find()

👉 Devuelve el primer elemento que cumple condición.

let numeros = [1, 3, 4, 6];

let par = numeros.find(n => n % 2 === 0);

console.log(par); // 4


✔ No modifica
✔ Devuelve un elemento (o undefined)

🔥 Resumen mental rápido

🔴 Mutan → cambian el array:
push, pop, shift, unshift, sort, reverse

🟢 No mutan → crean nuevo resultado:
map, filter, find, toSorted, forEach
